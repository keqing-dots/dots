#!/usr/bin/env bash

missing=()
for dep in "$@"; do
  command -v "$dep" >/dev/null 2>&1 || missing+=("$dep")
done
(( ${#missing[@]} == 0 )) && exit 0
echo "deps: missing dependencies: ${missing[*]}" >&2
exit 1
